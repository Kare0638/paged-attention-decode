--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.11.0+cu128
triton==3.6.0
pytest==9.1.1
numpy==2.5.1
ninja==1.13.0
matplotlib==3.11.1

# flashinfer-python is deliberately NOT pinned here -- only bench/bench_flashinfer.py
# and tests/test_flashinfer_adapter.py need it (an external baseline comparison, not
# one of this project's own kernels). Install it separately if you want that
# comparison; see README.md's Reproduce section for the install + verify steps.
